# Required: OpenAI API key for TTS generation
OPENAI_API_KEY="sk-..."

# Optional: Basic Auth credentials for access restriction
# If both are set, the app will require authentication
BASIC_AUTH_USER="admin"
BASIC_AUTH_PASSWORD="your-password-here"
